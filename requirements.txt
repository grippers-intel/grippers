# Host 코드 macOS(Apple Silicon) 의존성 — Python 3.11 전용.
#
# 파이썬 3.11 고정은 원본(Windows)과 같은 이유다. 2026-08-26 Host 팀 실측:
#   3.14 -> geti-sdk 설치 불가 (requires_python <=3.13)
#   3.13 -> pip 이 옛 geti-sdk 2.1.0 으로 되돌아가고 Pillow 소스빌드에서 깨진다
#   3.11 -> geti-sdk 2.13.1 이 그대로 깔린다
#
# 2026-08-28 에 macOS 15.7.7 / arm64 (Apple M1 Pro) 에서 그대로 재현했다.
# OpenVINO 는 macosx_11_0_arm64 휠이 있고, 장치 목록에 CPU 가 잡힌다:
#
#   openvino 2025.4.1   available_devices = ['CPU']   FULL_DEVICE_NAME = Apple M1 Pro
#
# 설치 (Homebrew 파이썬 기준):
#   brew install python@3.11
#   /opt/homebrew/opt/python@3.11/bin/python3.11 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt

geti-sdk==2.13.1
opencv-python==4.14.0.94
numpy==2.3.5
matplotlib==3.11.1

# 자연어 지시(fetch/organize, instruction_resolver.py) — 선택적, 없어도
# 나머지 기능은 정상 동작(ANTHROPIC_API_KEY도 따로 필요)
anthropic==1.3.0

# host/mac_local_yolo_view.py 전용(맥에 직접 물린 카메라로 YOLO 실시간 확인,
# tools/mac_camera_view.py --live 의 대안) — try/except 없이 하드 import라
# 없으면 이 스크립트 하나만 ModuleNotFoundError로 죽는다. run_mission.py를
# 포함한 나머지 전체 파이프라인은 이것 없이도 정상 동작.
ultralytics==8.4.138
torch==2.13.0
torchvision==0.28.0

# host/window_layout.py, host/mac_live_view.py 전용(--display 1 확장화면
# 창 배치) — try/except로 감싸져 있어 없어도 에러는 안 나지만, 창 배치가
# 조용히 OS 기본 위치로 떨어진다(위 "macOS 에서 달라지는 것" 절과 같은
# 유형의 조용한 실패).
pyobjc-framework-Cocoa==12.2.2
pyobjc-framework-Quartz==12.2.2

# 시험용 (tests/)
pytest==9.1.1
