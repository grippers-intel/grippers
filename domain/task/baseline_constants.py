"""Pi 미션의 수치 상수 — 실측 완료분과 TODO를 한곳에 모은다.

값의 출처를 세 등급으로 구분한다. 등급을 섞어 쓰면 무엇이 검증된 값이고
무엇이 지어낸 값인지 알 수 없게 된다.

  실측  — 하드웨어로 재서 확인한 값. 그대로 믿어도 된다.
  지시  — 사용자/팀이 정한 설계값. 실측은 아니지만 결정된 값이다.
  TODO  — **아직 모른다.** `None`으로 두어 쓰는 쪽이 반드시 걸리게 한다.

TODO를 0이나 그럴듯한 숫자로 채우지 않는 이유는 "모르면 실패"라는 이 저장소의
관례와 같다 — 지어낸 값으로 도는 것처럼 보이게 만들면 실기에서 원인을 못 찾는다.

⚠️ 2026-08-26 팀 확정으로 **좌표계 상수가 전부 사라졌다.** 물체 좌표, 차량
좌표와 방향, 경로는 Host가 소유한다. 여기 남은 것은 Pi가 자기 센서로 재고
자기 팔로 집행하는 값뿐이다. 좌표 상수를 다시 여기 넣고 싶어지면, 그건
Host의 일이 Pi로 새는 신호다.
"""

# ── 파지 (사용자 지시) ─────────────────────────────────────────────────────
# GRASP 진입 후 베이스가 곧장 전진하는 거리.
#
# 이 전진의 목적은 "물체 가까이 가는 것"이 아니라 **물체를 벌어진 턱 사이로
# 밀어 넣는 것**이다(사용자 설명 2026-08-26). 팔은 이미 바닥 교시 자세로
# 내려와 있고 그리퍼는 열린 상태다. 그 상태로 차체가 전진하면 물체가
# 손가락 사이로 들어온다 — 평행 턱의 벌어진 목이 좌우 자기정렬 효과까지 낸다.
#
# ⚠️ 매우 예민한 값이다. 사용자가 "여러 번 실측할 필요가 있다"고 명시했고,
# **5cm로 바뀔 수 있다.** 실기 조정 전까지 이 값을 근거로 다른 수치를
# 유도하지 말 것.
GRASP_CREEP_FORWARD_MM = 100.0  # 지시(잠정 — 50.0 후보)

# 교시 파지 자세가 전제하는 물체 중심 위치(차체 전면 기준 전방).
# floor_grasp_profiles.GRASP_OBJECT_CENTER_FORWARD_MM과 같은 값이다.
#
# Pi는 이 정렬을 **판정하지 않는다** — Host가 오버헤드로 보고 GRASP 명령을
# 보낼 때 이미 맞춰 놓는다. 여기 남겨 두는 이유는 GRASP 시퀀스가 전제하는
# 배치를 문서로 못박아 두기 위해서다.
GRASP_OBJECT_CENTER_FORWARD_MM = 190.0  # 실측 2026-08-20

# 열린 그리퍼 최대 너비. floor_grasp_policy.GRIPPER_MAX_SAFE_OPEN_MM와 같은 값.
GRIPPER_OPEN_MM = 168.0  # 실측 2026-08-20

# 바닥 교시 자세에서 턱 중심이 차체 전면 앞으로 나가 있는 거리.
#
# 2026-08-26 통주행에서 역산했다 — 물체를 190mm에 놓고 24mm 전진해 물렸으므로
# 턱 선은 약 166mm다. **n=1, 퀸 한 종류**이고 조작자가 "들어온 것 같다" 시점에
# 멈춘 값이라 보수적일 수 있다. 반복 실측 대상이다.
#
# ⚠️ 이 값은 **차체 전면 기준**이라 뎁스 카메라 값과 직접 비교하면 안 된다.
# 아래 JAW_LINE_DEPTH_FORWARD_M 주석 참고. 여기 남겨 두는 것은 기하를
# 문서로 붙잡아 두기 위해서다.
JAW_LINE_FORWARD_MM = 166.0  # 실측(n=1) 2026-08-26

# 같은 턱 선을 **뎁스 카메라가 읽는 값**으로 적은 것.
#
# 왜 위 값을 그대로 못 쓰는가: 뎁스 카메라의 전방 거리(observe_target의
# forward_m)는 클래스별 K를 base_link 기준 줄자 실측으로 잡았다. base_link는
# 차체 중심 근처이고 JAW_LINE_FORWARD_MM은 차체 **전면** 기준이라, 둘 사이에
# 차체 절반 길이만큼의 오프셋이 있다 — 잡으려는 영역의 깊이(전진 거리)보다
# 큰 오프셋이다. 프레임을 섞으면 조용히 틀린다.
#
# 오프셋을 따로 재서 환산하는 대신 **직접 잰다**: 실제로 물리는 위치에 물체를
# 놓고 observe_target의 forward_m을 읽으면 그 값이 곧 턱 선이다. 중간 변수가
# 없어 오차가 하나 줄어든다(바구니 정지 거리를 라이다 판독값으로 직접 잡은
# 것과 같은 이유).
#
# 이 값이 없으면 GRASP 정렬 판정을 하지 않는다 — 지어낸 프레임 변환으로
# 팔을 바닥에 내리느니 Host에 넘긴다.
JAW_LINE_DEPTH_FORWARD_M = None  # TODO

# 뎁스 카메라 광축과 턱 중심선의 좌우 어긋남(+ 왼쪽).
#
# 카메라가 정확히 가운데 달려 있다면 0이다. 아직 확인하지 않았으므로 0으로
# 두되, 좌우 보정이 늘 한쪽으로 치우친다면 여기부터 의심할 것.
DEPTH_LATERAL_TO_JAW_CENTER_M = 0.0  # 잠정 — 미확인

# servo 1 회전축에서 턱 중심까지의 수평 거리.
#
# 좌우 보정각을 atan2(오차, 이 값)으로 내므로 이게 없으면 **보정을 못 한다**.
# 지어낸 값을 쓰면 엉뚱한 각도로 턱을 돌리게 되므로 None으로 둔다 — 그동안은
# 치우친 물체를 Host에 넘긴다(grasp_alignment.judge 참고).
#
# 실측 방법: 바닥 교시 자세에서 servo 1을 알려진 각도만큼 돌리고 턱 중심이
# 좌우로 움직인 거리를 잰다. reach = 이동거리 / tan(각도).
SERVO1_AXIS_TO_JAW_MM = None  # TODO

# 이 안에 들면 "가운데"로 본다. 더 좁히면 매번 보정을 돌게 되고, 넓히면
# 사용자 지시("최대한 가운데")에서 멀어진다.
GRASP_CENTERING_TOLERANCE_M = 0.010  # 지시(잠정)

# ── 파지 판정 ──────────────────────────────────────────────────────────────
# 그리퍼가 무언가를 물고 있다고 볼 부하 하한.
# ⚠️ 근거 실측이 2026-08-18(n=25, 빈 최대 0.031)인데 2026-08-25 재실측에서
# 자세별 빈 부하가 0.0235~0.0430으로 흔들렸다. 재실측 대상이다.
#
# 2026-08-26 실기 참고값: 나이트·퀸 모두 파지 시 0.0626, 놓은 뒤 0.0313~0.0352.
LOAD_THRESHOLD = 0.04  # 실측(낡음)

# 그리퍼가 **비어 있다**고 볼 부하 상한. GRASP 조건 판정에 쓴다 —
# 이미 무언가를 물고 있는데 또 파지하러 내려가면 물고 있던 것을 떨어뜨린다.
# LOAD_THRESHOLD와 같은 값을 쓰되 이름을 나눠 둔다: 하나는 "쥐었다"를,
# 다른 하나는 "비었다"를 묻는 서로 다른 판정이다.
EMPTY_LOAD_CEILING = 0.04  # 실측(낡음)

# ── 라이다 기하 (2026-08-26 실측) ──────────────────────────────────────────
# grippers_base/basket_lidar_align.py와 같은 값이어야 한다. 도메인 계층이
# ROS 패키지를 import하지 않으므로 여기 한 번 더 적는다.
LIDAR_HEIGHT_M = 0.140          # 실측 2026-08-26 (이전 0.091은 오측)
LIDAR_TILT_DEG = 11.3           # 실측 2026-08-26 — 정면 아래로 기울어져 있다
LIDAR_MIN_RANGE_M = 0.020       # 실측 — LDRobot LD19 (RPLidar A1이 아니다)

# ── 바구니 ────────────────────────────────────────────────────────────────
# INSERT로 넘어갈 때 **라이다가 읽어야 하는 거리**다.
#
# 차체 전면 기준이 아니라 라이다 판독값 기준인 것이 핵심이다. 바구니는
# 높이마다 앞뒤 위치가 다른 면이라(앞턱이 위쪽 벽보다 15mm 앞으로 나와 있다)
# "차체 전면에서 바구니까지"라는 단일 거리가 **정의되지 않는다**. 2026-08-26에
# 판으로 잰 오프셋 4.54cm와 바구니로 잰 1.9cm가 2.6cm 어긋난 것이 그 증거다.
# 라이다 판독값으로 직접 판정하면 중간 변수가 사라져 오차가 하나 줄어든다.
#
# 검증된 창은 [0.130, 0.139]다(나이트 0.1386, 퀸 0.1301 — 둘 다 투하 성공).
# 창의 위쪽 끝에 두는 이유: 약 0.125m에서 빔이 테두리를 넘어 바구니를 통째로
# 놓친다. 아래가 절벽이므로 붙이는 쪽이 아니라 떨어지는 쪽이 안전하다.
BASKET_STOP_LIDAR_M = 0.140  # 실측 2026-08-26

# INSERT를 허용하는 라이다 판독 하한.
#
# 약 0.125m에서 빔이 테두리를 넘어 바구니를 놓친다. 그 아래로는 판독값이
# **커지는 방향으로** 틀리므로(뒤쪽 벽이나 무반사) 거리 하한만으로는 못 막는
# 종류의 실패다. 그래도 하한을 두는 이유는, 절벽 아래를 읽고 있다는 것 자체가
# "지금 보는 면이 우리가 교정한 그 면이 아니다"라는 신호이기 때문이다.
# 절벽에서 3mm 띄운다.
BASKET_MIN_LIDAR_M = 0.128  # 실측 근거 2026-08-26

# 정지 판정의 허용 폭. 이 안에 들어오면 INSERT 조건 충족으로 본다.
# 데드밴드 때문에 한 번의 최소 버스트가 15mm라 그보다 잘게 못 쪼갠다.
BASKET_STOP_TOLERANCE_M = 0.015  # 실측 근거 2026-08-26

# INSERT 시점에 허용하는 정렬 오차. 2026-08-26 실기에서 +2.82도로 투하에
# 성공했다. 그 실측치에 여유를 두되, 검증된 범위를 크게 넘기지 않는다.
BASKET_YAW_TOLERANCE_RAD = 0.087  # 5.0도 — 실측 2.82도 성공에 여유

# 바구니 테두리 높이.
BASKET_RIM_HEIGHT_M = 0.115  # 실측 2026-08-20

# 투하 자세에서 그리퍼 중심이 차체 전면 앞으로 뻗는 거리.
BASKET_DROP_REACH_FORWARD_MM = 200.0  # 실측 2026-08-20

# ── 링크 워치독 ────────────────────────────────────────────────────────────
# Host 명령이 이 사이클 수만큼 연속으로 안 오면 정지한다.
#
# "명령 없음"을 "직전 명령 유지"로 해석하면 링크가 끊겼는데 계속 굴러가는
# 사고가 난다. Host가 차량 제어를 소유한다는 것은 **Host가 말을 멈추면
# 차량도 멈춘다**는 뜻이기도 하다.
HOST_COMMAND_TIMEOUT_CYCLES = 3  # 지시

# ── 미션 진행 ──────────────────────────────────────────────────────────────
# 미션 한 번에 허용하는 파지 재시도 횟수.
MAX_GRASP_RETRY = 3  # 지시


def unresolved() -> dict:
    """아직 TODO인 상수 이름과 사유. 실기 투입 전 이 목록이 비어야 한다."""
    return {
        "GRASP_CREEP_FORWARD_MM": "100mm 대 50mm 미확정 — 반복 실측 필요",
        "LOAD_THRESHOLD": "2026-08-25 재실측에서 빈 부하가 흔들렸다 — 재실측 필요",
        "JAW_LINE_DEPTH_FORWARD_M": "미측정 — 없으면 GRASP 정렬 판정을 못 한다",
        "SERVO1_AXIS_TO_JAW_MM": "미측정 — 없으면 Pi가 좌우 보정을 못 한다",
    }
